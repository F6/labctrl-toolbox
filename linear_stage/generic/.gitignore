# server.config.json and hardware.config.json are generated files containing secrets and run-time parameters
# you can create these config files by copying *.default.config.json and filling the empty (secret) fields.
server.config.json
hardware.config.json
# config-new-user.json is autogenerated when running register.py to add new users.
config-new-user.json
